## 2025-02-14 - RxJava to Coroutines Stream Handling **Learning:** When replacing `flatMap { Observable.never<Unit>() }` with Coroutines `awaitCancellation()`, it is critical to explicitly handle or rethrow `CancellationException` if wrapped in a general `catch (e: Exception)` block. **Action:** Next time, ensure `catch (e: CancellationException) { throw e }` precedes any generic exception handler to avoid falsely logging standard coroutine cancellations as errors.
