## 2026-03-25 - Empty TODO Pattern **Learning:** Found a pattern of empty TODOs without dates or specific implementation plans, specifically asking to modify business logic ('// TODO: Also filter out manga here that are already presented') in SimilarHandler.kt. **Action:** Next time, identify and safely remove or ignore these outdated TODOs without modifying the underlying business logic.
## 2026-04-10 - Deprecation fixes involving File Paths **Learning:** When replacing `Intent.ACTION_MEDIA_SCANNER_SCAN_FILE` (which is deprecated in Android and required a `file://` URI), the replacement `MediaScannerConnection.scanFile()` requires a plain file path (String), NOT a URI. **Action:** Next time you migrate away from `ACTION_MEDIA_SCANNER_SCAN_FILE`, make sure to extract the `.absolutePath` from a `File` or `.path` from a `Uri`.
## 2026-04-15 - Use IntentCompat **Learning:** Found custom SDK branching suppressing deprecation warnings (`@Suppress("DEPRECATION")`) for `getParcelableExtra`. **Action:** Next time, replace manual SDK branching with `androidx.core.content.IntentCompat` methods to maintain modern, idiomatic code without deprecation warnings.
