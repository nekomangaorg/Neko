## 2024-05-24 - [Extracted complex when condition inside pager] **Learning:** Found a deeply nested `when` condition inside `HorizontalPager` that handled two completely different Layouts (`ComfortableGrid/CompactGrid` and `List`). Each layout had its own independent logic, like `rememberLazyGridState` vs `rememberLazyListState`. **Action:** Extracted these branches into their own components (`HorizontalGridDisplay` and `HorizontalListDisplay`) passing all state down.
