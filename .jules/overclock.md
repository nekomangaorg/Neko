## 2024-05-19 - [Library Flow Optimization] **Learning:** Unconditional Flow execution of heavy `MAX()` / `JOIN` SQL queries (like `getLastReadManga()` / `getLastFetchedManga()`) inside active `combine` ViewModels blocks the UI initialization for up to 10 seconds on large libraries (1000+ items). **Action:** Next time, wrap heavy aggregation queries inside `flatMapLatest` combined with active UI viewing preferences to ensure these queries are completely bypassed and return empty structures when the user is not actively sorting by those metrics.
