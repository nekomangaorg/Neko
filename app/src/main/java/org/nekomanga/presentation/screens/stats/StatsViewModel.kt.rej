--- app/src/main/java/org/nekomanga/presentation/screens/stats/StatsViewModel.kt
+++ app/src/main/java/org/nekomanga/presentation/screens/stats/StatsViewModel.kt
@@ -257,8 +257,9 @@

     private suspend fun getTracks(mangaList: List<LibraryManga>): List<Track> {
         return kotlinx.coroutines.coroutineScope {
-            mangaList.asSequence()
-                .mapNotNull { it.id }
+            mangaList
+                .asSequence()
+                .mapNotNull { it.id }
                 .chunked(900)
                 .map { chunk -> async { trackRepository.getTracksForMangaByIds(chunk) } }
                 .toList()
