# Custom Rules

- **Do NOT auto commit anything**: Never automatically run `git commit`. Keep all modifications unstaged in the working tree so that the user can review them and commit them manually.
- **Always generate a commit message**: At the end of every task involving code modifications, always output a draft commit message matching conventional commit standards.

